#include "main.h"
/**
 * add - adds two integers and return the result
 * @n1 first arg to add
 * @n2: second arg to add
 * Return: res -result
 */

int add(int n1, int n2)
{
	int res;

	res = (n1 + n2);
	return (res);
}
